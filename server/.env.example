PORT=8787
HOST=0.0.0.0
ALLOWED_ORIGINS=
TRUST_PROXY=false
ADMIN_USERNAME=admin
ADMIN_PASSWORD=请改成一个足够强的管理员初始密码
# 用于保持登录 token 在 PM2 重启后仍可验证；不填时使用 ADMIN_PASSWORD
SESSION_SECRET=请改成随机长字符串
DEBUG_ERRORS=false
